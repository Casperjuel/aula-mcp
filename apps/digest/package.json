{
  "name": "@aula-mcp/digest",
  "version": "0.1.0",
  "private": true,
  "description": "Daily Danish digest of Aula beskeder + opslag, delivered to Slack.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "bun src/index.ts",
    "build:bin": "bun build --compile --outfile bin/aula-digest src/index.ts"
  },
  "dependencies": {
    "@aula-mcp/aula-auth": "workspace:*",
    "@aula-mcp/aula-client": "workspace:*"
  }
}
